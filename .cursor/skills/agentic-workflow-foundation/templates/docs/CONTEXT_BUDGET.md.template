# Cursor セッション引き継ぎガイド（Context Budget Auto-Handoff）

<!-- このファイルは agentic-workflow-foundation スキルの生成物。統一設計書 / manifest / templates を更新して再生成する。 -->

> **目的**: 長時間セッションによる AI 回答品質劣化（Lost in the Middle）を防ぐ自動引き継ぎ機構の使い方。

## 生成根拠

このガイドは組織内の設計フレームワーク（immutable upstream SoT）と seed defaults から一時 resolved manifest を生成して出力している。SKILL 内部に前回実行状態は保存しない。

| 設計入力（役割） | 状態 |
|---|---|
{{#each framework.upstream_design_inputs}}
| {{this.role}} | {{this.status}} |
{{/each}}

## TL;DR

1. AI が `[CONTEXT_BUDGET=YELLOW]` を出したら、**現在の作業に区切りを意識**する
2. AI が `[CONTEXT_BUDGET=RED]` を出したら、**すぐに Cmd+N で新規チャット**を開く
3. 新規チャットで**何でもいい短文を 1 通送る** → 自動的に前セッションの作業内容が引き継がれる

{{framework.handoff.user_invisible}}。

## なぜ必要か

LLM には一度に参照できるコンテキスト量の上限があり、長いコンテキストでは中央付近の情報が相対的に弱く扱われる傾向がある。これが Lost in the Middle 問題で、長時間セッションでは序盤の制約、判断、方針が AI の注意から落ちやすくなる。

> {{framework.handoff.paper_citation}}

主な症状:

- 序盤に決めた方針を中盤以降で勝手に覆す
- 同じ間違いを繰り返す
- 重要な制約条件を忘れる
- 直前の設計判断と整合しない実装を始める

{{framework.handoff.degradation_example}}

{{framework.handoff.purpose}}。

> Hook の全体フロー・判定優先順位・閾値の具体値・状態ファイル仕様は [`.cursor/hooks/README.md`](../.cursor/hooks/README.md) を参照。
> progress.jsonl の `kind=review_start` は workflow Step ⑤ のレビュー検証フェーズ開始を表す（書き込みは親境界。詳細は hooks/README.md）。
> 閾値の決定根拠・校正サイクル・将来拡張は [`docs/references/context-budget-internals.md`](references/context-budget-internals.md) を参照。

## ユーザーが実際にやること

| 警告 | やること |
|---|---|
{{#each framework.handoff.user_actions}}
| `{{this.warning}}` | {{this.action}} |
{{/each}}

新規チャットで短文を1通送れば、`session-bootstrap.sh` が `.cursor/.session/handoff-{session_id}.md` を自動注入する。複数の handoff が存在する場合は AskQuestion でどれを読み込むかユーザーに選択させる。例: 「続きをお願いします」「再開」「.」。

### Red 通知後にやってはいけないこと

各禁止事項には理由を併記する。理由まで理解しておくと、引き継ぎ失敗と劣化ゾーンでの継続を避けやすい。

{{#each framework.handoff.forbidden_after_red}}
- **{{this.action}}** — {{this.reason}}
{{/each}}

## handoff manifest パス規約（SoT）

- **書き出し先**: `{{framework.handoff.active_manifest_path}}`（Yellow / Red 共通で同一パスを上書き）
- **消費後**: `session-bootstrap.sh` が `{{framework.handoff.consumed_manifest_pattern}}` にリネームしてアーカイブ
- `.cursor/` 直下に独自ファイルを作成しない

### 並行セッション対応

handoff manifest はセッションごとに独立したファイル（`handoff-{session_id}.md`）として管理される。並行セッションが同一 manifest を上書きする問題は発生しない。

| 状況 | bootstrap の動作 |
|---|---|
| handoff 0 件 | 新規セッション（handoff なし） |
| handoff 1 件 | auto-inject + consumed リネーム（従来と同じ UX） |
| handoff 2+ 件 | `[CONTEXT_BUDGET_HANDOFF_SELECT]` マーカーを注入し、AI が AskQuestion でユーザーに選択させる |

2+ 件は並行セッションの handoff や、放棄されたセッションの stale handoff が残存する場合に発生する。ユーザーが 1 つを選択すると、選択分のみ consumed リネームされ、残りは後続セッションで消費可能。

## tracker と handoff manifest の責務境界

{{framework.handoff.manifest_responsibility}}。矛盾時は常に tracker が正。

> 責務境界の詳細表・プランファイル連携の具体例は [`docs/references/context-budget-internals.md`](references/context-budget-internals.md) を参照。

## handoff manifest

handoff manifest の必須セクション一覧は `manifest.yaml > framework.handoff.manifest_required_sections` が SoT。manifest 内のセクションは「記録」（セッション判断ログ — 参照のみ、却下した案を再提案しない）と「指示」（次セッションの推奨アクション — 行動として実行）に分かれる。

復旧時の行動制約（SoT: manifest.yaml > recovery_constraints）:

{{#each framework.handoff.recovery_constraints}}
- {{this}}
{{/each}}

ユーザー向けの最終応答には詳細な作業ログを出さず、Red 到達時は新規チャットへ移る案内を短く返す。

## トラブルシューティング

### 警告が出ない

- `jq` がインストールされているか確認: `which jq`（無ければ `brew install jq`）
- `.cursor/hooks/*.sh` に実行権限があるか確認: `ls -la .cursor/hooks/`（無ければ `chmod +x .cursor/hooks/*.sh`）
- `.cursor/hooks.json` で `stop` / `sessionStart` / `beforeSubmitPrompt` / `afterShellExecution` が登録されているか確認する

### 早すぎる警告に見える

2 指標は OR 判定なので、もう一方の指標が先に到達した可能性がある。短いやり取りが多い場合は prompt_count、大量ログや探索が多い場合は shell_bytes が先に到達する。state ファイルの中身で原因を確認できる。

```bash
cat .cursor/.session/*.json | jq .
```

### 新規チャットを開いたが引き継ぎされない

1. `.cursor/.session/handoff-*.md`（`handoff-consumed-*` を除く）が存在するか確認する
2. 既に consumed 済みなら `.cursor/.session/handoff-consumed-*` を確認する
3. 必要なら consumed manifest を `handoff-{session_id}.md` として復元し、もう一度 Cmd+N で新規チャットを開く

## 失敗モード

| ID | 症状 | 対応 |
|---|---|---|
{{#each framework.handoff.failure_modes}}
| `{{this.id}}` | {{this.symptom}} | {{this.mitigation}} |
{{/each}}

## 設計上の既知の制約

- Phase 1 では token% は preCompact 発火時にしか取得できない（毎ターン取得は Phase 2 の API 拡張が前提）
- compact が一度も起きないセッションでは token% の恩恵がなく、proxy fallback のみで動作する
- 短時間でも重い議論や大量ログで劣化が進む false negative は残る
- macOS で動作確認済み。Linux / Windows は別途検証が必要
- session handoff の内容品質は AI が作る `handoff-{session_id}.md` の品質に依存するが、包括的構造化スナップショット（turns.jsonl + tracker + state）により、manifest が書けなかった場合でも fallback 注入が可能

> preCompact スナップショット / manifest 骨格の機械生成 / 注入サイズ上限 / スナップショット突合 /
> compact-events.jsonl 校正 / 校正サイクル / MemPalace 共存 / 将来拡張候補の詳細は
> [`docs/references/context-budget-internals.md`](references/context-budget-internals.md) を参照。

## チェックリスト

新メンバーの初回セットアップ:

- [ ] `jq` がインストールされている（`which jq`）
- [ ] `.cursor/hooks/*.sh` に実行権限がある（`ls -la .cursor/hooks/`）
- [ ] `.cursor/hooks.json` に session 系 Hook が登録されている（`afterAgentResponse` / `afterAgentThought` 含む）
- [ ] 数ターン会話して `.cursor/.session/{session_id}.json` が作られることを確認する（`cat .cursor/.session/*.json | jq .prompt_count`）
- [ ] Red 通知後は Cmd+N で新規チャットを開く運用を理解する

## 参考リンク

| 種類 | パス |
|---|---|
{{#each framework.handoff.references}}
| {{this.label}} | `{{this.path}}` |
{{/each}}
