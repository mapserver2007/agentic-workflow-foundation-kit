# 技術スタック（Domain 層サマリ）

<!-- このファイルは agentic-workflow-foundation スキルの生成物。直接編集せず manifest.yaml（tech_stack）を更新して再生成する。 -->

> 本ファイルは `manifest.yaml > tech_stack` から生成された Domain 層サマリ。`tech_stack` は Phase 1.6 で [`.cursor/docs/{{project.tech_stack_design_filename}}`](../.cursor/docs/{{project.tech_stack_design_filename}}) を取り込んだ結果であり、下流の Design Doc / AI レポート生成は本一覧を技術制約として参照する。

## 技術スタック一覧とバージョン方針

> {{tech_stack.note}}
>
> 各技術が対応する品質ゲート（G-GEN / G-BUILD / G-LINT / G-TEST）の内訳は [`docs/QUALITY_GATE.md §1.1 package script contract`](QUALITY_GATE.md) を参照。

| レイヤ | 技術 | バージョン方針 | 備考 |
| --- | --- | --- | --- |
{{#each tech_stack.items}}| {{this.layer}} | {{this.technology}} | {{this.version_policy}} | {{this.note}} |
{{/each}}
