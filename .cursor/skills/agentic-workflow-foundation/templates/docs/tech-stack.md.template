# 技術スタック（Domain 層サマリ）

<!-- このファイルは agentic-workflow-foundation スキルの生成物。直接編集せず manifest.yaml（tech_stack）を更新して再生成する。実固定バージョンの SoT は package.json / wrangler.jsonc。 -->

> 本ファイルは `manifest.yaml > tech_stack` から生成された Domain 層サマリ。`tech_stack` は Phase 1.6 で [`.cursor/docs/TECHNOLOGY_STACK_UNIFIED_DESIGN.md`](../.cursor/docs/TECHNOLOGY_STACK_UNIFIED_DESIGN.md) を取り込み、実 `package.json` / `wrangler.jsonc` があれば実態を優先して更新する。下流の Design Doc / AI レポート生成は本一覧を技術制約として参照する。

## 技術スタック一覧とバージョン方針

> {{tech_stack.note}}

| レイヤ | 技術 | バージョン方針 | 備考 |
| --- | --- | --- | --- |
{{#each tech_stack.items}}| {{this.layer}} | {{this.technology}} | {{this.version_policy}} | {{this.note}} |
{{/each}}
