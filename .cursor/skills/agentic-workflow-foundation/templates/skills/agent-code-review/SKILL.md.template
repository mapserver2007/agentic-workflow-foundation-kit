---
name: agent-code-review
description: What - GitHub PR 上のレビューコメント（自動レビューツール / 人間レビュアー）から指摘を取得し、各指摘の妥当性をリポジトリ内の証跡で検証して、判定 3 段階（修正必須 / 修正推奨 / 修正不要）+ Ask First 属性フラグでグループ承認ループを回して GitHub に返答する。初回レビュー対応（Mode A）と追加レビューコメント対応（Mode B）の 2 つの Mode を持つ。Use when - 「PRレビュー対応」「レビュー指摘の検証」「レビューコメントに返答」「コードレビュー検証」「PR 追加レビューコメント対応」「最新レビューコメント取得して返答」「追加コメント対応」等の指示、PR URL / `#数値` 単独貼付 + 「レビュー / 追加 / コメント / 返答」等のキーワード。Do NOT use for - (1) PR 作成依頼、(2) PR 起票前の実装フェーズ、(3) JIRA / Confluence 等 GitHub 外プラットフォームのコメント対応。
---

<!-- このファイルは agentic-workflow-foundation スキルの生成物。直接編集せず manifest.yaml を更新して再生成する。 -->

# Agent Code Review

GitHub PR 上のレビューコメント（自動レビューツール / 人間レビュアー）から指摘を取得し、各指摘の妥当性を検証して GitHub に返答するワークフロー。

**Progressive Disclosure** に従い、詳細手順・コマンド集・テンプレート・チェックリストは `references/` 配下に分割している（L3）。

> **関連スキル**: プロジェクト固有スキルと連携可能

---

## 前提条件

スキル起動直後に以下を自動チェックし、すべて揃わない場合は実行を中断する。

```bash
test -x bin/github-pr-reviews-safe && test -x bin/github-pr-comment-safe && test -x bin/github-pr-reply-safe || echo "GitHub wrapper not found"
```

- `bin/github-pr-reviews-safe` がスキル実行（Phase 2）で生成済み
- `bin/github-pr-comment-safe` がスキル実行（Phase 2）で生成済み
- `bin/github-pr-reply-safe` がスキル実行（Phase 2）で生成済み

> wrapper は GitHub App installation token を内部で発行し、AI に token を露出しない。GitHub Apps のセットアップは [references/gh-commands.md §0](references/gh-commands.md) を参照。`gh` CLI や `GITHUB_TOKEN` 環境変数は不要。

詳細チェックリストは [references/checklist.md §0](references/checklist.md) を参照。

---

## レビューレポート

レポート出力設定: **{{code_review.report.enabled}}**
レポート出力先: `{{code_review.report.output_dir}}`

- `true` かつ **進行中タスクレポートが存在する場合**: `{output_dir}/{task-report-stem}-review.md` にレビューレポートを保存
- `true` かつ **進行中タスクレポートが存在しない場合**（単独実行）: レビューレポートファイルは作成しない。チャットにレビュー結果サマリを出力するのみ
- `false` の場合: チャットにレビュー結果サマリを出力するのみ

レビュー結果は全指摘 1 ファイルでまとめて出力する。これは人間がコミット前に AI の修正判断を確認するための資料。

- **フォーマット**: [references/review-report-format.md](references/review-report-format.md) を参照（完全テンプレート + 判定カテゴリ別記述要件 + Gotchas / ADR 起票候補ブロック）

### 命名解決規則

レビューレポートのファイル名は、進行中タスクレポートから決定論的に導出する。`{task-report-stem}` = タスクレポートの拡張子なしファイル名（例: `TICKET-123-fix-auth-bug`）。

| 経路 | 命名 |
|---|---|
| ワークフロー経由（タスクレポートあり） | `{task-report-stem}-review.md` |
| 単独実行（タスクレポートなし） | レポートファイルを作成しない（チャットサマリのみ） |

**解決手順（Step 3 骨格作成の直前に実行）**:

1. `{{code_review.report.output_dir}}/` 直下を Glob し、`*-review.md` 以外の `.md` ファイル（= タスクレポート）を列挙する
2. **1 件**: そのファイルの stem に `-review.md` を付加してレビューレポートのファイル名を決定する
3. **複数件**: PR 番号で照合を試み、一意に特定できなければユーザーに選択を求める
4. **0 件**: 単独実行と判定し、レビューレポートは作成しない（レポート出力設定を `false` 相当として扱う）

**Mode B でのレポート検索**:

1. `{{code_review.report.output_dir}}/` 直下の `*-review.md` を Glob する
2. 各ファイルのヘッダ `- PR: #N` 行を Read し、PR 番号が一致するファイルを特定する
3. **1 件一致**: そのファイルを `StrReplace` で更新する
4. **0 件**: エラーとして原因を通知し中断する（フォールバックはしない）
5. **複数件**: ユーザーに選択を求める

### 段階的作成（レポート出力設定 `true` 時）

レビューレポートは Step 7 で一括作成するのではなく、**レビュー証跡が発生するたびに段階的に作成・更新**する。中断耐性（ディスク永続化によるセッション中断からの復元）・Mode B 互換性（既存ファイルを StrReplace で更新する前提）・ユーザー可視性のための設計。

| タイミング | 操作 | 更新内容 |
|---|---|---|
| Step 3 完了時 | `Write`（新規作成） | メタ情報 + 取得した指摘一覧の骨格。ヘッダ統計は `TBD`。`## レビュー完了承認` は `承認状態: レビュー進行中` |
| Step 4 各指摘判定完了時 | `StrReplace`（更新） | 該当指摘の判定結果・Blind所感・自己反論結果・検証結果を記入し、分類グループへ移動 |
| Step 5-6 `apply` 実行時 | `StrReplace`（更新） | アクション実行記録・修正内容・GitHub返信内容 |
| Step 5-6 `reply_only` / `pending` / `discuss` 時 | `StrReplace`（更新） | アクション実行記録・保留セクション即時更新 |
| Step 7（最終確定） | `StrReplace`（確定） | ヘッダ統計の `TBD` → 確定値、`承認状態: 承認取得済み`、完全性の最終検証 |

> **コミットポリシー**: レビューレポートの git commit はせず、Step ⑥ archive コミットにまとめて含める。`.coderabbit.yaml` の `path_filters` でレビューレポートは自動レビュー対象外のため、コミットタイミングの制約はない。

---

## ワークフロー

### Step Gate Matrix（踏み外し防止の最上位ゲート）

> **本スキルの本質はコメント内容の長期保持ではなく、状態遷移の踏み外し防止**である。各 Step は到達条件・参照義務・未達時の禁止・退出条件で囲い、ステップを飛ばす / 順序を誤る / 未承認で投稿する事故を構造的に防ぐ。レビューコメント本文は打ち返し後は不要になるため保持を前提にせず、必要時は Step 3 再取得 / レビューレポート / GitHub 状態から復元する。
>
> 各 Step の詳細手順は本文の該当節を SoT とする。本マトリクスは「**進んでよいか / 何を読むべきか / 何を禁止するか**」の早見表。

| Step | 入口条件（これが揃うまで開始禁止） | Read 必須 | 未達 / 未読時の禁止 | 次へ進む条件（退出条件） | 戻り先 |
| --- | --- | --- | --- | --- | --- |
| **1 Mode 選択** | スキル起動 + wrapper 前提条件 OK | — | PR 未作成のまま後続へ進む | Mode（A/B）+ PR 番号確定 | 前提 NG → 中断 |
| **2 自動レビュー待機**（A のみ） | Mode A | — | タイムアウト時に Step 3 以降へ進む / **Step 7 直行** | レビュー検出 | タイムアウト → **中断・ターン終了**（再実行待ち） |
| **3 指摘取得** | Mode 確定（B は Step 2 スキップ） | — | `author.login` フィルタ / 自動レビューツールのラベル取得 | 未解決・非 outdated・author 非依存で全件取得 + ID/種別記録 | 取得 0 件 → 完了経路でなく状況確認 |
| **4 妥当性検証** | Step 3 取得済み | **[`judgment-protocol.md`](references/judgment-protocol.md) 全文（突入時必須）**（+ challenge / catalog 詳細時 [`self-rebuttal.md`](references/self-rebuttal.md)） | `[REPO]` 出典外で判定 / Blind 省略 / 自己反論省略 / challenge を反転要請と解釈 | 全指摘で `judgment_final` 確定 + §4.3 記録完了 | — |
| **5-6 グループ承認ループ** | Step 4.3 記録済み | **[`approval-loop.md`](references/approval-loop.md) 全文（突入ごと必須）** | 未読でグループ提示・アクション分類・コード修正・GitHub reply 投稿・`pending`/`discuss` 記録 | 全指摘が `apply`/`reply_only`/`pending`/`discuss着地` に分類され実行済み | 新規コメント検出 → Step 3 |
| **Post** | Step 5-6 完了 | — | 保留 1 件以上 / ゲート未 PASS で Step 6.7 へ進む | `apply` 有時 Quality Gate PASS + **保留 0 件** | ゲート失敗 → 該当グループ修正へ |
| **6.7 完了承認** | Post で保留 0 件 | **[`review-completion.md`](references/review-completion.md) 全文（着手前必須）** | 未読で AskQuestion / Step 7 | 6.7-A: 再取得→AskQuestion 提示後**ターン終了** | 新規コメント → Step 3 |
| **7 レポート最終確定** | **6.7-B で承認回答受領済み** | — | 承認なしで実行 / Step 7 完了前に final response | レビューレポート最終確定（承認記録更新 + ヘッダ統計確定 + 完全性検証） | 「新規取得」選択 → Step 3 |

> **共通の投稿可否ルール（全 Step 横断 / 暗記必須）**: GitHub reply を投稿してよいのは `apply` / `reply_only` 集合のみ。`pending` と未着地 `discuss` には reply もコード修正もしない（「勝手に対応不要返答」防止の核心）。

### Step 1: Mode Selection + PR 情報の確認

冒頭スコープに従い、本スキルは **PR 作成依頼を扱わない**。スキル起動直後に AskQuestion で 2 つの Mode と PR 状態を確認する:

| Mode | 用途 | 後続 Step |
| --- | --- | --- |
| **Mode A: 初回レビュー対応** | PR 作成直後 / これから自動レビューを待つ | Step 2 → Step 3（全件取得） → Step 4 → Step 5-6（グループループ） → Step 6.7（レビュー完了承認） → Step 7 |
| **Mode B: 追加レビューコメント対応** | 既に Mode A を 1 回以上完了し、新たな未解決コメントが付いた | **[references/mode-b-reminder.md](references/mode-b-reminder.md) を Read で再注入** → Step 2 をスキップ → Step 3（未解決のみ） → Step 4 → Step 5-6 → Step 6.7（レビュー完了承認） → Step 7 |

PR 状態のサブ確認:

- **(a) PR は作成済み（PR 番号 / URL を入力）** — そのまま選択した Mode の後続 Step へ
- **(b) PR 未作成** — 本スキルを **中断**し、ユーザーに PR 作成を案内する。PR 作成完了後に本スキルを再起動する（再起動後は Mode A から）

(a) の場合の番号受取・URL からの番号抽出ルールは [references/gh-commands.md §1](references/gh-commands.md) を参照。

#### Mode B 突入時の最小再読込（Lost in the middle 対策）

同一セッション継続時にコンテキスト中盤の判定ルール / グループループ順序が retrieval 弱化する現象（Lost in the middle）を緩和するため、Mode B 選択直後に [references/mode-b-reminder.md](references/mode-b-reminder.md) を Read ツールで再注入する。SKILL.md 全体や他の references/ の再読込は不要。

PR 番号を記録して Step 2（Mode A）または Step 3（Mode B）へ。

### Step 2: 自動レビュー待機（Mode A のみ）

> Mode B では本 Step を **スキップ**し、Step 3 へ直接進む。

自動レビューツール（CodeRabbit 等）のレビューを `bin/github-pr-reviews-safe` でポーリング待機する。

- **ポーリング間隔**: 30 秒 → 60 秒 → 以降 60 秒
- **タイムアウト**: PR 作成から最大 10 分

タイムアウト時は **Step 3 以降に進まず、本スキルを中断してターンを終了する**（Step 7 への直行はしない / レビューレポートも作成しない）。次の再開案内を提示する:

```
自動レビューツールのレビューが 10 分以内に検出されませんでした。本スキルを中断します。
- 自動レビューツール固有のトリガーで手動レビューを実行するか、レビュー投稿を待ってから
- 本スキル（agent-code-review）を Mode A で再実行してください（Step 2 から再ポーリングします）
```

> **設計意図**: タイムアウト経路で Step 7 に直行すると、Step 6.7（レビュー完了承認）を経由せずに完了処理へ進む抜け道になる。中断・再実行待ちに倒すことで、Step 7 に到達するのは必ず Step 6.7 で承認を取得した経路のみに限定する。

ベースブランチが `main` / `release/*` 以外で自動レビューが走らない場合は自動レビューツール固有のトリガーで手動レビューを実行する。

ポーリングクエリ・手動トリガーの詳細は [references/gh-commands.md §2](references/gh-commands.md) を参照。

### Step 3: レビュー指摘の取得（レビュアー非依存）

`bin/github-pr-reviews-safe` で全レビュースレッドを取得し、以下をフィルタ:

- `isResolved: false`
- `isOutdated: false`
- **author.login によるフィルタは行わない**（自動レビューツール / 人間 / その他 bot を区別せず全て取得）

> **Mode A / Mode B の挙動差**: クエリ条件は両 Mode 共通（`isResolved=false isOutdated=false` で未解決のみ取得）。Mode A は PR 全期間の未解決指摘、Mode B は前回 Mode A 完了後に追加された未解決指摘のみが残るため、同じクエリで自然に「新規分のみ」が取得できる（前回対応分を保持する追加メタデータは不要）。

各指摘について以下を記録:

- レビューコメント ID（URL の `discussion_r{数値}` から抽出）
- ファイルパス / 行番号
- 指摘本文の主要部分
- **レビュアー種別**: `AutoReview` / `Human` / `OtherBot`（自動レビューツールの author.login で判別 → AutoReview、それ以外で `author.__typename == "User"` → Human、それ以外 → OtherBot）
- 「Prompt for AI Agents」テキスト（**自動レビューツールのみ提供する場合あり / 人間・他 bot は不在のため空欄**）

#### レビュアー種別による検証起点の分岐（§4.1 と連動）

| レビュアー種別 | 検証起点 | 適用追加バイアス |
| --- | --- | --- |
| AutoReview | 「Prompt for AI Agents」テキスト（提供される場合）または指摘本文 | catalog P1〜P7（自動レビューツールのミスパターン） |
| Human | **指摘本文そのもの** | catalog P8（人間レビュアーの主観混入） |
| OtherBot | 指摘本文そのもの | catalog P1〜P8 を準用（パターン一致時のみ） |

> **自動レビューツールのラベル取得禁止**: 自動レビューツールが付与する `Critical` / `Major` / `Minor` / `Trivial` / `Quick win` 等のラベルは**不正確である前提**のため、本 Step では取得・記録しない（指摘本文の主要部分から抽出するのも禁止）。判定段階の決定（修正必須 / 修正推奨 / 修正不要）は Step 4.2 で AI が単独で行う。

> **人間レビュアー指摘の扱い**: 人間レビュアー指摘は自動レビューツール指摘と**同一の判定フロー（§4.2 / §4.2.5）を適用**する。「Prompt for AI Agents」が無いため、§4.1 の検証起点は「指摘本文そのもの」に置き換える。判定根拠への混入バイアスは catalog P8 で構造的に防御する。

クエリ全文・パース方法・ID 抽出ロジックは [references/gh-commands.md §3](references/gh-commands.md) を参照。

#### Step 3 完了時: レビューレポート骨格作成（レポート出力設定 `true` 時）

指摘取得完了後、**命名解決規則**（§「レビューレポート > 命名解決規則」）に従いレビューレポートのファイル名を解決する。

- **タスクレポートが存在する場合**: レビューレポートファイル（`{{code_review.report.output_dir}}/{task-report-stem}-review.md`）を `Write` で新規作成する。Mode B の場合は既存ファイルを `StrReplace` で更新する（ヘッダの Mode を `B` に変更、ヘッダ統計を `TBD` に再初期化、`## レビュー完了承認` を `未承認（新規コメント取得待ち）` に戻す）。
- **タスクレポートが存在しない場合**（単独実行）: レビューレポートファイルは作成しない。以降の Step 4〜Step 7 ではレポート更新操作をスキップし、Step 7 でチャットにサマリのみ出力する。

骨格にはメタ情報（PR 番号・Mode・ブランチ）+ 取得した全指摘を `## 未分類（判定待ち）` セクションに配置する。フォーマット詳細は [references/review-report-format.md](references/review-report-format.md) を参照。

### Step 4: 各指摘の妥当性を検証

> **Hard Stop: `judgment-protocol.md` 読込必須**: Step 4 突入時に [references/judgment-protocol.md](references/judgment-protocol.md) を Read ツールで全文読込する。未読のまま判定を開始することは禁止。読込後、処理予定またはレビューレポートのメモに `judgment-protocol.md 読込済み` を記録してから判定へ進む。

完全手順（コード検証 §4.1 / 判定フロー §4.2 / 自己反論 §4.2.5 / 検証記録 §4.3）は [references/judgment-protocol.md](references/judgment-protocol.md) が SoT。以下は **常時保持する不変条件**（SoT を Read しなくても守る）:

#### 不変条件（常時適用 / 暗記必須）

- **`[REPO]` 出典制約**: 判定根拠として使用できるのは `[REPO]`（実コード / `AGENTS.md` / `.cursor/rules/01-critical-constraints.mdc` / コーディング規約 / `DECISIONS.md` / `QUALITY_GATE.md` / `GOTCHAS.md` / 類似既存実装）のみ。`[CR]`（指摘本文の言い回し）/ `[USER]`（ユーザー発言の語気）は判定根拠として使用禁止
- **Blind first read 必須**: 指摘本文・「Prompt for AI Agents」を読む**前**に該当ファイルを白紙状態で読み、`[REPO blind]` プレフィクス付き 1 行所感を記録
- **自己反論必須**: §4.2 ①〜④ で `judgment_initial` を出した直後、`judgment_final` を出す前に Phase 2 自己反論（最強反論 3 件 + `[REPO]` 出典スタンプ必須）→ Phase 3 評価（反転 / 確定+rebuttal / 引き分け→個別承認）を必ず実施
- **`judgment_final` は 3 段階**: 修正必須 / 修正推奨 / 修正不要（+ `ask_first` 属性フラグ）
- **デフォルト「修正不要」起点**: 初期判定は「修正不要」とし、昇格には ③ フィルタ通過 + ④ 実害シナリオの記述が必要。迷ったら修正不要に倒す
- **自動レビューツールのラベル非依存**: `Critical` / `Major` 等のラベルは取得・記録・引用すべて禁止。判定は AI が `[REPO]` 証跡のみで単独で行う
- **Phase 3.5 ユーザー challenge 再評価ロック**: challenge phrase（「本当に？」「誤りでは」等）を「反転要請」と解釈禁止。`[REPO]` 新証跡なしに判定を反転しない
- **`discuss` と Phase 3.5 の経路分離**: challenge phrase 検出時は Phase 3.5 が優先発動し、`discuss` には入らない

#### Step 4 各指摘判定完了時: レビューレポート更新（レポート出力設定 `true` 時）

各指摘の `judgment_final` 確定後、レビューレポートを `StrReplace` で更新する。`## 未分類（判定待ち）` セクションから該当指摘を分類グループ（修正必須 / 修正推奨 / 修正不要）へ移動し、判定結果・Blind 所感・自己反論結果・検証結果を記入する。詳細は [references/judgment-protocol.md](references/judgment-protocol.md) Phase 4 を参照。

### Step 5-6: 判定グループ承認ループ（横断アクション指定子対応）

> **Hard Stop: `approval-loop.md` 読込必須**: Step 5-6 突入前に [references/approval-loop.md](references/approval-loop.md) を Read ツールで全文読込する。未読のまま Pre 判定サマリ提示・アクション分類・コード修正・GitHub reply 投稿を開始することは禁止。読込後、処理予定またはレビューレポートのメモに `approval-loop.md 読込済み` を記録する。

完全手順（アクション語彙 / 入力モード / 全体構造 / Pre / グループ提示 / 共通ルール / Post / Quality Gate + 保留残数チェック / アクション別挙動 / グループ別追加確認）は [references/approval-loop.md](references/approval-loop.md) が SoT。以下は **常時保持する不変条件**（SoT を Read しなくても守る）:

#### 不変条件（常時適用 / 暗記必須）

- **アクション語彙 5 種**: `apply`（修正+返信）/ `reply_only`（返信のみ）/ `pending`（保留）/ `discuss`（追加情報提供+Q&A）/ `override:{修正必須|修正推奨|修正不要}`（判定変更+アクション再指定必要）
- **`pending` / `discuss` への返信禁止**: `pending`（保留）と未着地 `discuss` には **GitHub reply を投稿しない**（コード修正もしない）。GitHub reply を投稿できるのは `apply` / `reply_only` のみ
- **個別指定時の未言及 = `pending`**: 具体的 No 指定を 1 つでも含む個別指定モードでは、言及なし項目は全て `pending`（reply 投稿なし）。「勝手に対応不要返答」を構造的に防ぐ核心ルール
- **Hard Stop**: 承認未取得で GitHub reply 投稿禁止。全ゲート PASS かつ保留 0 件まで Step 6.7 に進まない
- **`discuss` と Phase 3.5 の経路分離**: challenge phrase 検出時は Phase 3.5 が優先発動し `discuss` には入らない
- **`override` はアクション再指定が必要**: 判定変更後に自動で `apply` 等にしない
- **Post Quality Gate**: `apply` を 1 件以上実行した場合 `{{session.verification.gate_command}}` を実行。**保留残数チェックは常時必須**

### Step 6.7: レビュー完了承認（必須・人間承認ゲート）

> **Hard Stop: `review-completion.md` 読込必須**: Step 6.7 着手前に [references/review-completion.md](references/review-completion.md) を Read ツールで全文読込する。未読のまま AskQuestion 提示・Step 7 のレポート作成に進むことは禁止。

**発動条件**: Post で **保留 0 件** を確認した場合のみ到達（保留 1 件以上なら次セッション持ち越し）。

**状態遷移（不変条件 / SoT を Read しなくても守る）**:

- **Step 6.7-A**: GitHub 再取得（新規あれば Step 3 へ）→ `AskQuestion` で完了承認を提示 → **ターン終了**（Step 7 に進まない）
- **Step 6.7-B**: 「承認する」選択の**次ターン冒頭で Step 7 を強制実行**（Step 7 完了まで final response 禁止）。「新規取得」選択時は Step 3 へ

### Step 7: レビューレポート最終確定

> **前提**: Step 6.7-B で承認回答受領済み。承認なしでは本 Step に進まない。

**状態遷移（不変条件）** — 手順全文・サマリテンプレは [references/review-completion.md](references/review-completion.md) が SoT:

- レポートファイルは Step 3 で骨格作成済み・Step 4〜Step 6 で段階的に更新済み。本 Step では最終確定のみを行う
- `## レビュー完了承認` の `承認状態` を `レビュー進行中` → `承認取得済み` に更新し、承認日時を記録する
- ヘッダ統計（指摘総数・アクション集計・保留件数）の `TBD` を確定値に置換する
- 完全性の最終検証（全指摘のアクション記録・Gotchas/ADR 候補ブロック・ビルド結果の充足）を行う
- チャットにサマリを出力する
- 例外フロー（完了報告後の追加コメント → Mode B 再起動）も review-completion.md に集約

---

## Mode B リマインダ要約

Mode B 選択直後に [references/mode-b-reminder.md](references/mode-b-reminder.md) を Read ツールで再読込し、判定ルール・Step 順序・Hard Stop をコンテキスト末尾に再注入する。SKILL.md 全体や他の references/ の再読込は不要。

> **Lost in the middle 対策**: 同一チャット継続時にコンテキスト中盤の判定ルール / グループループ順序が retrieval 弱化する現象を緩和する。フル再読込は不要で、`mode-b-reminder.md` だけで再注入が完了する。

---

## L3 リファレンス一覧

| ファイル | 内容 |
| --- | --- |
| [references/judgment-protocol.md](references/judgment-protocol.md) | Step 4 判定プロトコル詳細（コード検証 §4.1 / 判定フロー §4.2 / 自己反論 §4.2.5 / 検証記録 §4.3） |
| [references/checklist.md](references/checklist.md) | 前提条件 + 各 Step の完了チェック項目 |
| [references/review-report-format.md](references/review-report-format.md) | レビューレポートの完全フォーマット + Gotchas / ADR 候補ブロック |
| [references/gh-commands.md](references/gh-commands.md) | wrapper コマンドリファレンス（`bin/github-pr-reviews-safe` / `bin/github-pr-comment-safe` / `bin/github-pr-reply-safe`） |
| [references/reply-templates.md](references/reply-templates.md) | GitHub 返信テンプレート（**文体原則** + 修正必須 / 修正推奨 / 修正不要 + Ask First マーク付き項目用の追加文言） |
| [references/self-rebuttal.md](references/self-rebuttal.md) | §4.2.5 自己反論ステップ詳細（Phase 2 反論記述例 / catalog P1〜P9 詳細表 / Phase 3.5 強制手順 + 失敗パターン例） |
| [references/approval-loop.md](references/approval-loop.md) | Step 5-6 承認ループ詳細（アクション別挙動 / 個別指定モード処理予定確認 / グループ別追加確認） |
| [references/group-presentation-templates.md](references/group-presentation-templates.md) | グループ提示・AI 処理予定確認テンプレ集（Pre 判定サマリ / Step 5-6.1 / 5-6.2 / 5-6.3 / 個別指定モード処理予定確認） |
| [references/review-completion.md](references/review-completion.md) | Step 6.7 レビュー完了承認 / Step 7 レポート最終確定詳細（6.7-A 再取得 / 6.7-B 強制再開 / Step 7 の最終確定手順 + サマリテンプレ） |
| [references/mode-b-reminder.md](references/mode-b-reminder.md) | Mode B 再注入専用リマインダ（Lost in the middle 対策 / 判定ルール・Step 順序・Hard Stop の要約） |

---

## 関連ドキュメント

- `docs/GOTCHAS.md` — 失敗事例記録（直接追記先）
- `docs/DECISIONS.md` — ADR 運用ガイド（起票候補の参照先）
