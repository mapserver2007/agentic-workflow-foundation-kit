# {{project.name}}

<!-- このファイルは agentic-workflow-foundation スキルの生成物。直接編集せず manifest.yaml を更新して再生成する。 -->

{{project.one_liner}}

> AGENTS.md を SoT とし二重管理を防止する（統一設計書 §12 Layer 1）。Claude Code / Codex CLI 環境では `@AGENTS.md` 参照が解決されない場合があるため、以下に最小コンテキストを記載する。

@AGENTS.md

## Key Commands

```bash
{{project.quality_gate.gen_cmd}}     # OpenAPI 由来の生成
{{project.quality_gate.build_cmd}}   # ビルド
{{project.quality_gate.lint_cmd}}    # lint
{{project.quality_gate.test_cmd}}    # テスト
```

## Boundaries（要約）

- **Never**: 無断 push / commit / `--force` push / 保護ブランチ直 push / `gh pr merge` / `git reset --hard origin/*`
{{#each project.boundaries.never_extra}}- **Never**: {{this}}
{{/each}}{{#each project.boundaries.always}}- **Always**: {{this}}
{{/each}}
> 完全な Boundaries / Session Protocol / Documentation Navigation は `AGENTS.md` を参照。Claude Code で `@AGENTS.md` が解決されない場合は `cat AGENTS.md` で確認する。
